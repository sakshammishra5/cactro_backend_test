<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Issue</title>
    <style>
        body {
            background-color: #0d1117;
            color: #c9d1d9;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }

        .issue-container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #161b22;
            padding: 20px;
            border-radius: 6px;
        }

        .issue-title {
            margin-bottom: 20px;
        }

        .issue-title input {
            width: 100%;
            padding: 8px;
            background-color: #0d1117;
            border: 1px solid #30363d;
            color: #c9d1d9;
            border-radius: 6px;
        }

        .issue-description {
            margin-bottom: 20px;
        }

        .issue-description textarea {
            width: 100%;
            height: 300px;
            padding: 8px;
            background-color: #0d1117;
            border: 1px solid #30363d;
            color: #c9d1d9;
            border-radius: 6px;
            resize: vertical;
        }

        .toolbar {
            background-color: #21262d;
            padding: 8px;
            border-radius: 6px 6px 0 0;
            display: flex;
            gap: 4px;
        }

        .toolbar button {
            background: none;
            border: none;
            color: #c9d1d9;
            cursor: pointer;
            padding: 4px 8px;
        }

        .toolbar button:hover {
            background-color: #30363d;
            border-radius: 4px;
        }

        .submit-btn {
            background-color: #238636;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 6px;
            cursor: pointer;
        }

        .submit-btn:hover {
            background-color: #2ea043;
        }
    </style>
</head>

<body>
    {{> nav}}
    <div class="issue-container">
        <h2>Create new issue</h2>
        <h3>Raise an issue for the repository:-{{repoName}}</h3>
        <form action="/github/issues" method="post">
            <div class="issue-title">
                <label for="title">Add a title <span style="color: #f85149;">*</span></label>
                <input type="text" id="title" name="title" placeholder="Title" required>
                <input type="hidden" name="repoName" value="{{repoName}}">
            </div>
            <div class="issue-description">
                <label for="description">Add a description</label>
                <textarea id="description" name="description" placeholder="Type your description here..."></textarea>
            </div>
            <button class="submit-btn" type="submit">Submit new issue</button>
        </form>
    </div>

    <script>
        // Basic contenteditable functionality for textarea (simplified)
        document.querySelector('textarea').addEventListener('focus', function () {
            this.setAttribute('contenteditable', true);
        });
    </script>
</body>

</html>